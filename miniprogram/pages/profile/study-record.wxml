<view class="header">
  <view class="header-top">
    <view class="back-btn" bindtap="goBack">‹</view>
    <text class="header-title">学习记录</text>
    <view class="header-placeholder"></view>
  </view>
</view>

<view class="record-page with-fixed-header">
  <view wx:if="{{list.length == 0 && !loading}}" class="empty-card">
    <view class="empty-icon">📚</view>
    <view class="empty-title">暂无学习记录</view>
    <view class="empty-text">开始刷题后，这里会记录你的答题轨迹。</view>
  </view>

  <view class="record-list">
    <view class="record-card" wx:for="{{list}}" wx:key="id">
      <view class="card-topline">
        <text class="pill {{item.is_correct ? 'correct' : 'wrong'}}">{{item.is_correct_text}}</text>
        <text class="muted">{{item.answered_at_text}}</text>
      </view>
      <view class="question-title">{{item.title}}</view>
      <view class="question-content">{{item.content}}</view>
      <view class="info-panel">
        <view>
          <view class="info-label">我的答案</view>
          <view class="info-value">{{item.my_answer || '未作答'}}</view>
        </view>
        <view class="score-badge">+{{item.score || 0}}</view>
      </view>
    </view>
  </view>

  <view wx:if="{{loading}}" class="loading">加载中...</view>
  <view wx:if="{{finished && list.length > 0}}" class="loading">没有更多了</view>
</view>
